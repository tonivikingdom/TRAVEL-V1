# 项目级 Codex 工作规范

## 最高前提
这是用户授权的新建空白项目。旧仓库不是起点；不得默认连接、克隆或改造`tonivikingdom/travel-planner-v2`。在用户确认的新目录中工作，远程不明确时只询问新仓库地址。不要移动/清理任何未知文件。

## 每次任务必须先写模型建议
在任务说明和阶段计划开头填写：推荐模型、推理强度、备选、理由、升级条件。本包默认P0架构用GPT-5.6 Sol / Extra High（xhigh），明确施工子任务可Luna / Max；以客户端实际可用选项为准。
不能声称提示词已自动切换模型；当前配置无法确认就说未知。未来发任务要重新核对官方可用性，不永久沿用旧型号。

## 阅读顺序
1. 当前授权的`prompts/`任务。
2. `docs/00_需求优先级与变更记录.md`。
3. `docs/02_首轮开发要求_v1.0.md`。
4. 本阶段相关的03/04/05/06文档。
5. `docs/08_未决事项与阶段闸门.md`。
6. 完整产品基线01仅用于理解背景，不能当“全部立即实现”清单。
`archive/`只存历史，不用于覆盖当前规则。

## 范围纪律
- 每次只执行用户授权的一个阶段。P0完成必须停止，不能顺手做P1。
- 先核对目录、Git、remote、版本、依赖与环境。缺失是事实，不猜。
- Domain与UI/网络/ORM解耦；只共享业务契约，不强迫多端共用UI。
- 只实现当前需要的可扩展边界，不为协作/微服务/原生端做过度设计。
- 未决项只暂停对应能力；记录问题与影响，向用户一次问一个关键点。
- 不擅自新增产品入口、字段、商业服务、付费API调用或全局默认分钟数。

## 数据与业务底线
不编地点、班次、价格、时区、用户轨迹、订单状态、测试结果。fixture必须明示SYNTHETIC，dev/test专用。
计划、预计、实际、用户要求和来源分开；已执行事实不被重排覆盖。
Query/Preview不改正式行程；Adopt需权限/版本/幂等/事务验证。
不自动订票、叫车或对外改预约；拒绝/关闭建议进入安静辅助。
无网不展示过时行程；单一Provider失效局部未知；位置缺失不能由服务器猜。

## 安全底线
邀请制多用户；管理员不默认访问他人内容。私有资源逐请求授权，不能只检查角色。
会话可服务端撤销；不提交密钥、token、真实.env、数据库或附件。
对象存储私有；默认不做附件内容提取；保留必要格式/大小/授权校验。
只使用隔离测试数据；reset须明确dev/staging，不动未知或生产数据库。

## GitHub是阶段完成的一部分
所有正式开发：feature branch → 真实测试 → commit → push → PR → 等待检查。
禁止直推main、force push、未经授权合并、擅自切换远程、删除他人修改。
空仓库没有main首提交时，用户先建最小基线或单独授权bootstrap；本规范不授权main业务提交。
若push/PR权限不可用，汇报BLOCKED、保留成果；不得宣称“已同步”。

## 测试与汇报
lint/typecheck/test/build按阶段实际运行。关键集成须真实PostgreSQL；环境不能跑就标未验证。不要skip/弱化断言凑绿灯。
最终中文汇报简短，细节写`docs/status/`：阶段、分支、SHA、PR、改动、测试证据、限制、停止点。
不能将“文档已写”“夹具测试通过”“真实Provider已接通”“跨平台已支持”混为一谈。

## 未来UI任务
本轮不做正式UI。未来桌面和移动各自设计/测试；共享契约、设计token和已确认SVG，不用桌面缩放冒充移动端。遵守用户视觉居中偏好，具体视觉以后单独验收。
