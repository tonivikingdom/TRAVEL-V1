# P1B2 NotificationEvent 与 ObjectStorage 基础边界

- 当前状态：实现完成；Draft PR
  [`#4`](https://github.com/tonivikingdom/TRAVEL-V1/pull/4) 等待最终验收/合并
- 正式 base：`200e26ad85d6b41533d87fb8841729cfd5a2a688`
- 推荐模型 / 强度：GPT-5.6 Sol / High
- 安全独立复核：GPT-5.6 Sol / Extra High
- 明确小修备选：GPT-5.6 Luna / Max
- 实际使用模型与强度：未知（客户端实际配置无法从仓库证据确认）
- P2、正式 UI、Push、真实云存储 Provider 与 Production 部署均未授权

## 基线事实

- P1B1 已 Squash Merge，main commit 为
  `200e26ad85d6b41533d87fb8841729cfd5a2a688`。
- main CI [`Run 35322043421`](https://github.com/tonivikingdom/TRAVEL-V1/actions/runs/35322043421)
  的 `verify` 与 `Compose verification` 均为 success。
- 远程 `feature/p1b1-jobs-mail` 已删除。

## NotificationEvent

- PostgreSQL 持久化 owner、kind、dedupeKey、用户可见 title/body、occurredAt、createdAt 与
  dismissedAt；`(ownerUserId, dedupeKey)` 唯一约束保证同一用户逻辑事件幂等。
- `GET /notifications` 认证后只返回 actor 自己的数据，按 `createdAt + id` 稳定倒序游标分页；
  已 dismiss 的记录默认仍返回并携带 `dismissedAt`。
- `POST /notifications/:id/dismiss` owner-only 且幂等。ADMIN 不能读取或 dismiss 其他用户事件。
- 创建只开放给可信 application / worker 用例，没有公开 `POST /notifications`；本阶段不发明
  风险语义、不实现 Push、偏好或正式 UI。

## ObjectStorage 与 StoredObject

- `packages/storage` 提供流式 `put/open/delete/stat/exists` port；Local Filesystem adapter 仅供
  Development/Test，未来 provider 可替换而不改变 application 契约。
- `StoredObject` 只保存 owner、服务端随机 key、PENDING/READY/FAILED/DELETED 状态、展示元数据、
  实际 byte size 与 SHA-256；数据库不存二进制，P2 前不关联虚假 Trip。
- Local adapter 限定隔离 root 与 UUID key，拒绝 traversal、绝对/编码路径和 symlink；先写私有
  temporary file，校验实际大小/hash 后 atomic rename，失败清理且不标 READY。
- owner 取自认证 actor，ADMIN 无跨用户能力；READY 才可读，DELETED 不可读，删除可重试且幂等。
- read/delete 使用 `(ownerUserId, objectId)` 查询；不存在与属于其他 owner 的对象统一返回
  `NOT_FOUND`，包括 ADMIN，避免通过 ID 探测私有对象存在性。
- 单文件大小、用户总量和 MIME allowlist 均来自环境配置。总量预留通过 PostgreSQL owner 级
  advisory transaction lock 串行化，防止并发明显超卖。
- O-09 正式产品数值仍待上线阶段确认。Development/Test 的 10 MiB/100 MiB 默认值仅为
  SYNTHETIC 工程默认；Staging/Production 必须显式配置且真实 provider 未实现时保持不可用。

## 验证状态

- 本机 `format`、ESLint、TypeScript、单元测试与 build 已通过；单元测试为
  11 files / 48 tests。
- PostgreSQL integration 为 5 files / 61 tests，已在 GitHub CI PostgreSQL 17 通过；本机没有
  本任务专用 PostgreSQL/Docker，因此本机未验证 PostgreSQL integration 或 Compose。
- CI [`Run 35328518826`](https://github.com/tonivikingdom/TRAVEL-V1/actions/runs/35328518826)
  的 `verify` 与 `Compose verification` 均为 success。
- Compose verification 已扩展 SYNTHETIC 私有对象写入、API 容器重建后的文件与元数据持久性、
  幂等删除和不可读取验证；不使用全局 prune，只在 finally 清理本次隔离项目资源。

## 上线前 PENDING reservation 硬闸门

当前 `reserve PENDING → 写 ObjectStorage → READY/FAILED` 不是跨资源事务。正式
Attachment/upload API 或 Staging/Production ObjectStorage 启用前，必须实现 stale PENDING
reservation 的 expiry/reconciliation，避免进程崩溃后永久占用用户 quota，并处理 provider 侧
orphan temporary/object cleanup。本轮没有公开上传入口，也不提前扩展 cleanup Job 或上传协议。

## 停止点

P2 未授权。没有正式 Attachment HTTP 上传/下载、Trip 关联、文件解析、S3/R2/OSS、Push 或
Production 部署。O-09 正式限额仍未决定；Staging/Production ObjectStorage provider 仍为
`OBJECT_STORAGE_PROVIDER_UNCONFIGURED`。
